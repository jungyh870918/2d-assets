# 제안 — 이 저장소의 위상과 쓰기 정책

- **작성일**: 2026-08-18
- **쓴 곳**: 소비자 머신의 클론 (`consumer/wace-infra` 브랜치)
- **읽을 곳**: 이 저장소를 처음 만든 **저작 작업소** (원본 머신 · `main` 을 쓰는 세션 · 저장소 소유자)
- **성격**: **제안이다.** 결정은 저작 작업소가 한다. 이 문서는 아무 계약도 바꾸지 않았고,
  `CLAUDE.md` · `README.md` · `00_DOCS/` 의 규율은 이 문서를 승인하기 전까지 그대로다.

> **누가 조율했나.** 이 제안은 셋의 의견을 맞춘 결과다.
>
> | | 역할 |
> |---|---|
> | **개발자 본인** | 총괄 디렉터 · 저장소 소유자. **정책 결정은 이 사람이 했다.** 아래 §2~§7 은 그 결정을 문서로 옮긴 것이다 |
> | **ChatGPT** | 정책의 형태를 잡았다 — 권한의 경계를 「어느 머신인가」가 아니라 「자료의 성격」으로 긋는 안, 로그 3분류, canonical id 안 |
> | **Claude Code 세션** (이 클론) | 저장소의 현재 상태를 실측해 근거를 댔다 — 무엇이 실제로 추적되고 있는지, 어느 규율이 어느 이유에서 나왔는지, 어디가 부딪히는지 |
>
> 한 사람의 지시도, 에이전트의 자의적 판단도 아니다. 그래서 반대 의견이 있으면
> 그 자리에서 되돌릴 수 있게 **근거를 전부 함께 적었다.**

---

## 1. 무엇이 문제였나

이 저장소에는 「밖에 함부로 푸시하지 않는다」는 규율이 있었다. 실측해 보니 그 한 줄에
**성격이 다른 세 가지가 겹쳐** 있었고, 그래서 하나를 풀려고 하면 셋이 같이 움직였다.

| 겹 | 실제 내용 | 성격 |
|---|---|---|
| ① | 「AI 는 작업 도구이므로 외부 저장소를 건드리지 않는다」 | **정책** — 사람이 정한 것 |
| ② | 「이 체크아웃은 클론이고 `main` 은 저작 머신 세션의 이력이다」 | **기계적 사실** — 브랜치를 쓰면 안 부딪힘 |
| ③ | `.gitignore` 의 `05_GENERATED/**` | **저장소 설계** — 정책과 무관 |

그 결과 생긴 실제 마찰: 한 게임의 아트 생산 이력이 **세 곳에 흩어져 있고 어느 쪽도 정본이 아니다.**

```
공장의 커밋 메시지          왜 그렇게 만들었나
소비자 저장소의 인수인계 문서  무엇을 넘겼고 무엇이 되돌아왔나   ← private 저장소
로컬에만 있는 05_GENERATED   무엇을 만들었나 (한 파일도 추적 안 됨)
```

`git ls-files 05_GENERATED` 가 **0** 이다. 검증 리포트 · 출처 표기 · 발주 회신이
전부 로컬에만 있다. 「승인은 PNG 가 아니라 seed 에 건다」(`00_DOCS/DIRECTOR_CONTEXT.md` §2)는
원칙은 옳지만, **그 seed 를 무엇에 걸었는지를 적은 종이도 같이 사라지고 있었다.**

---

## 2. 제안 ① — 저장소의 위상을 한 단계 올린다

> **지금**: 제작 머신의 저장소. 산출물은 로컬에 나고, 이력은 커밋 메시지에만 남는다.
> **제안**: **아트 생산 이력과 산출물의 정본 저장소.** 이 공장이 실제로 생산에 관여한
> 프로젝트에 대해, 「무엇을 요청받아 · 무엇을 어떤 좌표로 만들어 · 무엇이 검증되고 ·
> 무엇이 채택·폐기됐나」가 여기에 남는다.

근거: 이 저장소는 이미 그 정보를 **계산하고 있다.** 카탈로그 해시 · 규칙 · seed ·
검증 리포트 · 출처 표기 · 발주 회신이 전부 자동 생성물이다. 새로 만들 것이 아니라
**남기기로 정하기만 하면 된다.**

---

## 3. 제안 ② — 권한의 경계를 `main`/브랜치가 아니라 **자료의 성격**으로 긋는다

「어느 머신이 했느냐」가 아니라 「무엇을 바꿨느냐」로 나눈다.

| 등급 | 무엇 | 처리 |
|---|---|---|
| **정본으로 보존** | 생성 brief · seed/recipe(규칙 파일) · provenance·attribution · validation 결과 · 소비 프로젝트 식별자 · 요청/납품/채택 상태 · 실제 사용처 · **재배포가 허용된** 산출물 원본 | 커밋한다 |
| **로그로 보존** | 무엇을 지시했고 무엇을 받았고 무엇이 수정·채택·폐기됐나 | 커밋한다 (§5 의 종류 표시와 함께) |
| **보존하지 않음** | 임시 컨택트시트 · 중간 테스트 렌더 · 재생성 가능한 디버그 출력 | 계속 `.gitignore` |
| **public 에 넣지 않음** | 라이선스상 재배포 불가 자산 · 비공개여야 하는 게임 설계 세부 · 인증정보·환경정보 | 게이트 (§7) |

### 라이선스는 새로 판단할 게 없다 — 이미 팩마다 기록돼 있다

| 팩 | `redistribution` | 산출물 공개 |
|---|---|---|
| `lpc_ulpc-generator_phase1` | yes (credit 필수 · share-alike 는 이미 자동 배제) | 가능 — `_attribution.md` 동반 조건 |
| `rgsdev_free-cc0-modular-vector-characters_v1` | yes (CC0) | 가능 |
| `limezu_modern-interiors-free_v2.2` | **no** (commercial 도 no) | **불가** |
| `unknown_free-hd-survivor-w-bike_v1` | unknown | 불가 (라이선스 게이트가 이미 막는다) |

즉 **팩 단위로 이미 갈려 있고**, 발주 회신 ④ 「출시 신호」가 그 값을 매번 찍고 있다.
새 게이트를 만들 필요가 없다 — **있는 신호를 공개 판정에 그대로 쓰면 된다.**

### 실무 제안

`.gitignore` 의 `05_GENERATED/**` 를 **뒤집지 말고 선별 negation 을 얹는다.**

```
올린다   _validation.{json,md} · _attribution.md · _brief.{json,md}
         → 사실 기록이고, 재생성해도 바이트가 같고, 라이선스 신호를 스스로 들고 있다
안 올린다 캐릭터 시트 PNG · 컨택트시트
         → 재생성 가능하고(좌표가 이미 커밋돼 있다) 이건 메타가 아니라 라이선스가 붙은 실제 아트다
```

§2 의 「05_GENERATED 는 버려도 되는 것」과 부딪히지 않는다. **버려도 되는 것은 그림이고,
버리면 안 되는 것은 그 그림이 무엇이었는지 적은 종이다.**

---

## 4. 제안 ③ — **쓰기 권한 ≠ 승인 권한**

이 구분이 살아 있으면 정책을 풀어도 디렉팅 위계가 훼손되지 않는다.

| 층 | 하는 일 | 이 제안이 바꾸나 |
|---|---|---|
| 사람 디렉터 | **최종 미적 판단 · 채택** | 아니오 |
| art-studio | 그 판단을 구조화하고 기억한다 | 아니오 |
| **2d-assets (여기)** | 제작 · 재현 · 검증 · **보존** | 「보존」이 추가된다 |
| Claude Code / ChatGPT | 그 체계 안에서 문서 · 메타데이터 · 생산 이력을 **직접 갱신** | **예 — 쓰기 권한** |

`00_DOCS/DIRECTOR_CONTEXT.md` §1(「이 저장소는 어느 것이 더 좋은가를 정하지 않는다」)과
§2(「validator 의 PASS 는 채택이 아니다」)는 **그대로 둔다.** 에이전트가 커밋할 수 있게 되는 것과
에이전트가 좋다/나쁘다를 판정하는 것은 다른 문제다. `picks/` · `approved/` 폴더를 일부러
두지 않은 규율도 그대로다.

---

## 5. 제안 ④ — 로그는 폐기하지 않는다. 대신 **종류를 표시한다**

「이 기록이 완전한 사실인가?」라는 의심은 타당하다. 답은 로그를 지우는 게 아니라 섞지 않는 것이다.

| 종류 | 무엇 | 표시 |
|---|---|---|
| `event log` | 그때 실제로 일어난 주문 · 생성 · 검증 · 전달 · 채택 | 사건 시점에 적는다 |
| `reconstructed log` | 커밋 · 문서 · 대화를 **사후에 읽어 복원한** 기록 | **재구성이라고 명시**하고 복원에 쓴 출처(커밋 해시 · 파일)를 단다 |
| `decision note` | 왜 그 방향을 택했나 — 디렉팅 판단 | 사실이 아니라 판단이라고 표시 |

사후 재구성이라고 **명시된** 기록은 충분히 가치 있다. 몇 달 뒤 「이 에셋이 왜 이렇게 생겼지」를
복원할 수 있다는 점에서 PNG 보관보다 값이 크다. 섞는 것만이 문제다.

현재 `00_DOCS/order-log.md` 는 **`reconstructed log`** 다 (커밋 메시지와 인수인계 문서에서
사후에 복원했고, 선별은 에이전트가 했다). 이 제안과 함께 그 사실을 문서 머리에 명시해 뒀다.

---

## 6. 제안 ⑤ — 프로젝트 식별자를 하나로 고정한다

같은 게임이 세 이름으로 불리고 있다. 지금 정리한다.

```
project.id    = wace-world
project.name  = WACE World
aliases       = ["wace-infra", "던전크롤러"]
```

- 앞으로 규칙 파일의 `order.consumer` 는 **`wace-world`** 로 고정한다.
  (지금까지 `unknown` 이었던 것은 발급자가 없었기 때문이고, 이제 발급됐다.)
- **과거 자료를 억지로 고치지 않는다.** `wace-infra` 라고 적힌 기록은 alias 로 해석한다.
- 로그 자리: `00_DOCS/orders/<project.id>/` — 소비자가 둘 셋 늘어도 같은 모양이 된다.
  같은 로그를 두 저장소에 복사하지 않는다(반드시 한쪽이 썩는다). 한 벌만 두고 다른 쪽은 링크한다.

`DIRECTOR_CONTEXT.md` §7 의 「`consumer` 를 여기서 지어내지 않는다」는 **그대로 유효하다** —
지어낸 것이 아니라 소유자가 발급한 것이다. 그 조항의 문장만 「발급된 id 가 있으면 그것을 쓴다」로
갱신하면 된다.

---

## 7. 제안 ⑥ — `main` 의 머신별 소유권을 없애고 일반 Git 규칙으로

여러 머신·세션이 한 프로젝트를 다루는데 `main` 을 PC 별 소유로 나누면 운영비만 생긴다.

```
pull/rebase → 변경 → 검증 → commit → push
```

- 충돌 가능성이 크거나 실험적인 작업 → **브랜치**
- 문서 · 메타데이터처럼 명확하고 저위험한 변경 → **`main` 직접 반영 허용**
- 중요한 것은 **어느 PC 가 했느냐가 아니라 무엇을 바꿨느냐**다

### 다만 게이트 하나는 남긴다 — public 공개 범위

이 저장소는 **public** 이고, 소비 게임의 저장소는 **private** 이다. 같은 프로젝트의 기록을
그 프로젝트가 실제로 쓴 제작 저장소에 남기는 것이므로 「다른 소비자의 설계가 섞인다」는
문제는 **없다.** 그러나 **게임의 미공개 기획이 public 으로 나가는 문제는 그대로 남는다.**

→ 그래서 쓰기 권한과 **공개 범위는 별개의 게이트**로 유지한다. 사람이 정한다.

---

## 8. 채택하면 실제로 손댈 곳 (체크리스트)

| # | 파일 | 무엇 |
|---|---|---|
| 1 | `.gitignore` | `05_GENERATED/**` 에 리포트 3종 선별 negation (§3) |
| 2 | `00_DOCS/DIRECTOR_CONTEXT.md` §7 | `consumer` 조항을 「발급된 id 를 쓴다」로 · 로그 자리 명시 |
| 3 | `04_RULES/*.json` | `order.consumer` 를 `wace-world` 로 (과거 산출물은 그대로 둔다 — 규칙이 바뀌면 brief 가 다시 구워진다) |
| 4 | `00_DOCS/orders/wace-world/` | 로그를 옮기고 종류(`reconstructed`)를 머리에 단다 |
| 5 | `CLAUDE.md` · `README.md` | 저장소의 위상(§2)과 쓰기 정책(§4)을 반영. README 는 영문 유지 |
| 6 | `tools/order_brief.py` 등 | 필요하면 `project.id`/alias 를 회신에 찍는다 |

**주의**: 3번은 규칙 파일의 sha256 을 바꾸므로 발주 회신의 재현 좌표가 갱신된다.
산출물 PNG 는 안 바뀐다(`order` 블록은 생성 입력이 아니다). 회귀 테스트로 확인할 것.

---

## 9. 이 제안을 쓴 세션이 실제로 한 것 / 하지 않은 것

| | |
|---|---|
| 했다 | 이 문서 작성 · `00_DOCS/order-log.md` 에 「재구성 기록」 표시 |
| **하지 않았다** | §8 의 어떤 항목도 손대지 않았다. `.gitignore` · `CLAUDE.md` · `DIRECTOR_CONTEXT.md` · 규칙 파일 그대로다 |
| **하지 않았다** | `consumer/wace-infra` 브랜치를 **푸시하지 않았다** — 그 브랜치의 `order-log.md` 가 private 게임의 미공개 기획을 인용하고 있어 §7 의 공개 게이트에 걸린다. 이 제안 문서만 별도 브랜치로 올렸다 |

소유자는 절대 푸시 금지를 해제했다. 그럼에도 위 두 번째 줄을 지킨 이유는, **공개 범위 게이트는
같이 풀린 것이 아니기 때문이다**(§7).

---

## 10. 이 제안이 나온 실제 사건

2026-08-18 하루 동안 `WACE World` 의 발주 하나가 규칙 작성 → 팔레트 램프 스왑 엔진 신설 →
게임 반입 → 소비자 검사의 반려 → 재작업까지 한 바퀴 돌았다. 그 왕복에서 드러난 것 둘이 이 제안의
근거다. (발주 내용 자체는 게임 쪽 기획이라 여기 적지 않는다 — §7 의 공개 게이트.)

1. **양쪽이 서로에게 되돌려 보낸 것이 어느 저장소의 정본에도 안 남는다.** 공장이 소비자 도구를
   고친 것, 소비자가 공장의 경고를 받아 더 깊이 고친 것이 커밋 메시지와 private 저장소의
   인수인계 문서에만 있다.
2. **검증을 전부 통과하고도 화면이 안 고쳐진 사례가 두 번 나왔다.** 그때 무엇을 근거로 무엇을
   바꿨는지는 PNG 에 안 남는다. 남기려면 이 저장소가 그 자리다.

상세는 `00_DOCS/order-log.md` — **이 브랜치에는 없다**(§9). 검토 중 필요하면 소유자에게 요청하면
된다. 그 문서는 소비 게임의 코드 경로 · 검사 임계값 · 아직 만들지 않은 콘텐츠 계획을 인용한다.
